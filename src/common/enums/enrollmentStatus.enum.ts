export enum EnrollmentStatus {
  PENDING = 'pending',
  APPROVED = 'approved',
  PROCESSED = 'processed',
  TRANSFERRED = 'transferred',
  DECLINED = 'declined',
  CANCELED = 'canceled',
  REFUNDED = 'refunded',
}
